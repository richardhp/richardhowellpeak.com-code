<%= form_with(model: admin_blog_category, local: true) do |form| %>
  <% if admin_blog_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_blog_category.errors.count, "error") %> prohibited this admin_blog_category from being saved:</h2>

      <ul>
        <% admin_blog_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
