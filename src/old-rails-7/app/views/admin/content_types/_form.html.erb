<%= form_with(model: admin_content_type, local: true) do |form| %>
  <% if admin_content_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_content_type.errors.count, "error") %> prohibited this admin_content_type from being saved:</h2>

      <ul>
        <% admin_content_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :ordering %>
    <%= form.number_field :ordering %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
